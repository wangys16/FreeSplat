# @package _global_

defaults:
  - override /dataset: replica
  - override /model/encoder: freesplat
  - override /model/encoder/backbone: efficientnet
  - override /loss: [mse, lpips]

wandb:
  name: replica
  tags: [replica, 384x512]

dataset:
  image_shape: [384, 512]
  roots: [/home/wang/ssd/replica/ps]
  near: 0.5
  far: 15.
  view_sampler:
    num_context_views: 2

model:
  encoder:
    num_views: 2
    num_depth_candidates: 128

data_loader:
  train:
    batch_size: 1

loss:
  lpips:
    apply_after_step: 0
    weight: 0.05

trainer:
  max_steps: 300_001

train:
  depth_sup: true
  depth_mode: depth
